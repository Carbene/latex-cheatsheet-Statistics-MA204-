\section{Probability and Distributions}
\[\binom{n}{r} = \binom{n-1}{r} + \binom{n-1}{r-1} \]
\[\binom{-x}{r} = (-1)^r\binom{x+r-1}{r}\]
\[\binom{n}{r_1,r_2,\dots,r_k} = \binom{n}{r_1}\dots\binom{n-r_1-\dots-r_{k-1}}{r_k}\]
\[\binom{x}{r} = \frac{x(x-1)\dots(x-r+1)}{r!}\]
\[\binom{a}{0}\binom{b}{n} + \binom{a}{1}\binom{b}{n-1} + \dots + \binom{a}{n}\binom{b}{0} = \binom{a+b}{n}\]
\[\text{Bernoulli}(p) = p^x (1-p)^{1-x}\]
\[p\text{ | }p(1-p)\text{ | }1 - p + p e^t\]
\[\text{Binomial}(n,p) = \binom{n}{x} p^x (1-p)^{n-x} \]
\[ np \text{ | } np(1-p) \text{ | } (1 - p + p e^t)^n \]
\[\text{Poisson}(\lambda) = \frac{\lambda^x e^{-\lambda}}{x!}\text{ | }\lambda \text{ | }\lambda \text{ | } e^{\lambda (e^t - 1)}\]
\[\text{Geometric}(p) = (1-p)^{x-1} p \]
\[\frac{1}{p} \text{ | } \frac{1-p}{p^2} \text{ | } \frac{p e^t}{1 - (1-p)e^t}\]
\[\text{Uniform}(a,b) = \frac{1}{b-a} \]
\[ \frac{a+b}{2} \text{ | } \frac{(b-a)^2}{12} \text{ | } \frac{e^{tb} - e^{ta}}{t(b-a)}\]
\[\text{Normal}(\mu, \sigma^2) = \frac{1}{\sqrt{2\pi}\sigma} e^{-\frac{(x-\mu)^2}{2\sigma^2}}\]
\[\text{E}(Z^{2r+1}) = 0 \text{ | } \text{E}(Z^{2r}) = \frac{(2r)!}{(2^rr!)}\]
\[\mu \text{ | } \sigma^2 \text{ | } e^{\mu t + \frac{1}{2}\sigma^2 t^2}\]
\[\text{Gamma}(\alpha, \beta) = \frac{\beta^\alpha}{\Gamma(\alpha)} x^{\alpha-1} e^{-\beta x} \]
\[ \frac{\alpha}{\beta} \text{ | } \frac{\alpha}{\beta^2} \text{ | } \left(\frac{\beta}{\beta - t}\right)^\alpha\]
\[\text{E}(\ln(X)) = \frac{\Gamma'(\alpha)}{\Gamma(\alpha)} - \ln(\beta)\]
\[\theta = \frac{1}{\beta} \text{ | } \sum\Gamma(\alpha_i, \beta) = \Gamma(\sum \alpha_i, \beta)\]
\[ \Gamma(1,\beta) = \text{Exponential}(\beta) \text{ | } \Gamma(\frac{n}{2},\frac{1}{2}) = \chi^2_n\]
\[Y \sim \text{Gamma}(\alpha, \beta),cY \sim \text{Gamma}(\alpha, \frac{\beta}{c})\]
\[\text{Beta}(\alpha, \beta) = \frac{x^{\alpha-1} (1-x)^{\beta-1}}{B(\alpha, \beta)} \]
\[ \frac{\alpha}{\alpha + \beta} \text{ | } \frac{\alpha \beta}{(\alpha + \beta)^2 (\alpha + \beta + 1)} \text{ | } \emptyset\]
\[\text{B}(a,b) = \frac{\Gamma(a)\Gamma(b)}{\Gamma(a+b)} \]
\[\text{Beta}(1,1) = \text{U}[0,1] \text{ | }\mu_{r}' = \text{E}(X^{r})\]
\[\text{ZIP}(\phi,\lambda) \coloneq ZX \text{|} (1-\phi)\lambda \text{|} (1-\phi)\lambda(1+\phi\lambda)\]
\[Z = \text{Bernoulli}(1-\phi), X = \text{Poisson}(\lambda)\]
\[\text{M}_{\text{ZIP}}(t) = \phi + (1-\phi)e^{\lambda(e^t - 1)}\]
\[\frac{1}{2\pi\sigma_1\sigma_2\sqrt{1-\rho^2}} e^{-\frac{1}{2(1-\rho^2)}(\frac{(x-\mu_1)^2}{\sigma_1^2} - 2\rho\frac{(x-\mu_1)(y-\mu_2)}{\sigma_1\sigma_2} + \frac{(y-\mu_2)^2}{\sigma_2^2})}\]
\[X_1|X_2 \text{ | } \mu_1 + \rho\frac{\sigma_1}{\sigma_2}(x_2 - \mu_2) \text{ | } \sigma_1^2(1-\rho^2)\]
\[\text{Skewness} =\frac{\mu_{3}}{\sigma^3}\text{ | Kurtosis}  = \frac{\mu_{4}}{\sigma^4}\]
\[\text{P}(g(X) \geq c) \leq \frac{\text{E}(g(x))}{C} \]
\[ \text{P}(|X-\mu| \geq c\sigma) \leq \frac{1}{c^2}\]
\[\text{P}(|X-\mu| \geq c) \leq \frac{\text{E}(|X|^r)}{c^r} \]
\[ {\text{E}(XY)}^2 \leq \text{E}(X^2) \text{E}(Y^2)\]
\[\text{E}(g(X)) \geq g(\text{E}(X)), \forall \text{凸函数g}(x^2)\]
\[\text{E}(X) = \text{E}[\text{E}(X|Y)] \]
\[ \text{Var}(X) = \text{E}[\text{Var}(X|Y)] + \text{Var}[\text{E}(X|Y)]\]
\[\text{q分位数}\xi_{q} \coloneq \text{F}(\xi_{q}) = \text{P}(X \leq \xi_{q}) \geq q\]
\[\text{pgf} \coloneq \text{G}_{X}(z) = \text{E}(z^{X}) = \sum_{x \in \mathbb{S}}z^{x} \text{P}_{X}(X = x)\]
\[\text{Additivity : } \text{ N, Poisson, NB, Binomial}\]
\[IBF \coloneq f_{X}(x)\propto \frac{f_{X|Y}(x|y_0)}{f_{Y|X}(y_0|x)}\]